<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Finder-Search</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h2>Schools in <%=city%></h2>
    <div class="grid">
        <% schools.forEach(school=>{%>
        <div class="card">
            <h3><%=school.name%></h3>
            <p><%= school.description.substring(0,60)%>...</p>
            <a href="/school/<%=school._id%>">View Details</a>
        </div>
      <% })%>
    </div>
</body>
</html>